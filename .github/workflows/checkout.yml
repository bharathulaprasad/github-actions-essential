name: Create envfile

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
 
    steps:
    - uses: "DamianReeves/write-file-action@master"
      with:
          path: hello.txt
          contents: |
            echo "Hello World!"
          write-mode: append
