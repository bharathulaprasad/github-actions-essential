name: Create envfile

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
 
    steps:
    - uses: "finnp/create-file-action@master"
      env:
        FILE_NAME: "build/hello.txt"
        FILE_DATA: "file content"
